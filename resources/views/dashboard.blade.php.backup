<x-app-layout>
    <div class="py-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            {{-- Welcome Card --}}
            <div class="bg-gradient-to-r from-blue-600 to-purple-600 rounded-2xl shadow-lg p-8 mb-8 text-white">
                <h2 class="text-3xl font-bold mb-2">Selamat Datang, {{ auth()->user()->name }}!</h2>
                <p class="text-blue-100">Lanjutkan pembelajaran tahsin Al-Qur'an Anda</p>
            </div>

            {{-- Subscription Status --}}
            @php
                $subscription = auth()->user()->subscription;
                $hasSubscription = $subscription && $subscription->status === 'active';
            @endphp
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                @if($hasSubscription)
                    <div class="bg-green-50 border-2 border-green-200 rounded-2xl p-6">
                        <div class="flex items-center gap-3 mb-3">
                            <div class="bg-green-500 rounded-full p-2">
                                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                                </svg>
                            </div>
                            <div>
                                <h3 class="font-bold text-green-900">Langganan Aktif</h3>
                                <p class="text-sm text-green-700">Aktif hingga {{ $subscription->end_date->format('d M Y') }}</p>
                            </div>
                        </div>
                    </div>
                @else
                    <div class="bg-orange-50 border-2 border-orange-200 rounded-2xl p-6">
                        <div class="flex items-center justify-between gap-3">
                            <div>
                                <h3 class="font-bold text-orange-900 mb-1">Belum Berlangganan</h3>
                                <p class="text-sm text-orange-700">Aktifkan untuk akses penuh</p>
                            </div>
                            <a href="{{ route('student.subscription.index') }}" class="bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded-lg font-semibold text-sm transition-colors">
                                Berlangganan
                            </a>
                        </div>
                    </div>
                @endif

                {{-- Enrolled Class --}}
                @if($enrolledClass)
                    <div class="bg-blue-50 border-2 border-blue-200 rounded-2xl p-6">
                        <div class="flex items-center gap-3 mb-3">
                            <div class="bg-blue-500 rounded-full p-2">
                                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                                    <path d="M10.394 2.08a1 1 0 00-.788 0l-7 3a1 1 0 000 1.84L5.25 8.051a.999.999 0 01.356-.257l4-1.714a1 1 0 11.788 1.838L7.667 9.088l1.94.831a1 1 0 00.787 0l7-3a1 1 0 000-1.838l-7-3z"/>
                                </svg>
                            </div>
                            <div>
                                <h3 class="font-bold text-blue-900">Kelas Anda</h3>
                                <p class="text-sm text-blue-700">{{ $enrolledClass->name }}</p>
                            </div>
                        </div>
                        <a href="{{ route('student.my-class') }}" class="inline-block bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold text-sm transition-colors">
                            Lihat Kelas
                        </a>
                    </div>
                @endif
            </div>

            {{-- Lessons List --}}
            @if($enrolledClass && $lessons->count() > 0)
                <div class="bg-white rounded-2xl shadow-lg p-6">
                    <h3 class="text-xl font-bold text-gray-900 mb-6">Materi Pembelajaran</h3>
                    
                    <div class="space-y-4">
                        @foreach($lessons->take(5) as $lessonData)
                            @php
                                $lesson = $lessonData['lesson'];
                                $status = $lessonData['status'];
                                $isCompleted = $lessonData['isCompleted'];
                                $isCurrent = $lessonData['isCurrent'];
                                $isLocked = $lessonData['isLocked'];
                            @endphp

                            <div class="border-2 {{ $isCompleted ? 'border-green-200 bg-green-50' : ($isCurrent ? 'border-blue-200 bg-blue-50' : 'border-gray-200 bg-gray-50') }} rounded-xl p-4 {{ !$isLocked ? 'hover:shadow-md cursor-pointer' : 'opacity-60' }} transition-all"
                                 onclick="{{ !$isLocked ? "window.location='" . route('student.lessons.show', $lesson) . "'" : '' }}">
                                
                                <div class="flex items-center justify-between gap-4">
                                    <div class="flex items-center gap-3 flex-1">
                                        @if($isCompleted)
                                            <div class="bg-green-500 rounded-full p-2">
                                                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                                                </svg>
                                            </div>
                                        @elseif($isCurrent)
                                            <div class="bg-blue-500 rounded-full p-2">
                                                <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM9.555 7.168A1 1 0 008 8v4a1 1 0 001.555.832l3-2a1 1 0 000-1.664l-3-2z" clip-rule="evenodd"/>
                                                </svg>
                                            </div>
                                        @else
                                            <div class="bg-gray-300 rounded-full p-2">
                                                <svg class="w-5 h-5 text-gray-600" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"/>
                                                </svg>
                                            </div>
                                        @endif
                                        
                                        <div class="flex-1">
                                            <h4 class="font-bold {{ $isCompleted ? 'text-green-900' : ($isCurrent ? 'text-blue-900' : 'text-gray-700') }}">
                                                {{ $lesson->title }}
                                            </h4>
                                            <p class="text-sm text-gray-600">Materi {{ $lesson->order }}</p>
                                        </div>
                                    </div>
                                    
                                    @if(!$isLocked)
                                        <a href="{{ route('student.lessons.show', $lesson) }}" class="bg-{{ $isCompleted ? 'green' : 'blue' }}-500 hover:bg-{{ $isCompleted ? 'green' : 'blue' }}-600 text-white px-4 py-2 rounded-lg font-semibold text-sm transition-colors">
                                            {{ $isCurrent ? 'Lanjutkan' : ($isCompleted ? 'Ulangi' : 'Mulai') }}
                                        </a>
                                    @else
                                        <span class="text-sm text-gray-500">🔒 Terkunci</span>
                                    @endif
                                </div>
                            </div>
                        @endforeach
                    </div>

                    @if($lessons->count() > 5)
                        <div class="mt-6 text-center">
                            <a href="{{ route('student.my-class') }}" class="text-blue-600 hover:text-blue-700 font-semibold">
                                Lihat Semua Materi →
                            </a>
                        </div>
                    @endif
                </div>
            @endif

            {{-- No Class Message --}}
            @if(!$enrolledClass)
                <div class="bg-gray-50 rounded-2xl p-12 text-center">
                    <svg class="w-16 h-16 text-gray-400 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                    </svg>
                    <p class="text-gray-600 text-lg mb-4">Anda belum terdaftar di kelas manapun</p>
                    <a href="{{ route('student.classes.index') }}" class="inline-block bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
                        Lihat Kelas Tersedia
                    </a>
                </div>
            @endif
        </div>
    </div>
</x-app-layout>
