{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --optimize-autoloader --no-dev && php artisan config:cache && php artisan route:cache && php artisan view:cache && npm ci && npm run build"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan storage:link && php artisan optimize && php-fpm -y /assets/php-fpm.conf & nginx -c /assets/nginx.conf",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}